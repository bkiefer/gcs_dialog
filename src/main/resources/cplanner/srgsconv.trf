<da>#da -> # ^ :#da, # ! <da>.

<prop>#prop -> # ^ (#prop), # ! <prop>.

<number>#n: ^ !<what>-> # ^ <what>string2number(#n), # ! <number>.

<neg> ^ <what>#w: -> # ^ <what>concatenate("-", #w), # ! <neg>.