
initial:
if(robot.hasNewUser == true){
    // Todo create new user
    state == "phase_1";
}

phase_one:
if(state == "phase_1"){
    emitDA(#Inform(DRZGreet));
    if (user.areEyesOpen == false){
        emitDA(#Request(OpenEyes));
    }
    // new internal state wait_open_eyes
    // timeout wait open eyes

}